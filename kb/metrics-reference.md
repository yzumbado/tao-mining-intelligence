# Metrics Reference Guide

> **Auto-generated** from `lambda/src/processor/metrics.py` structured docstrings.
> **Last generated**: 2026-05-19 03:49 UTC
>
> **Do NOT edit this file manually.** Update the `Metric:` block in the source
> code and re-run: `python scripts/generate_metrics_reference.py`

---

## How to Read This Document

Each metric has:
- **Formula**: The exact calculation
- **Hypothesis**: What we believe this metric represents and why
- **Usefulness**: How this metric informs decisions (mining, staking, risk)
- **Status**: `PROVEN` | `HYPOTHESIS` | `NEEDS_VALIDATION` | `DEPRECATED`
- **Known Issues**: Current limitations
- **Assumptions**: Things that need validation

---

## Status Legend

| Status | Meaning | Action |
|--------|---------|--------|
| **PROVEN** | Validated against live outcomes or standard formula | Use for automated decisions |
| **HYPOTHESIS** | Logic sound, not validated against outcomes | Use for recommendations, flag uncertainty |
| **NEEDS_VALIDATION** | Known limitations or missing data | Display but don't act on |
| **DEPRECATED** | Replaced or found misleading | Remove from rankings |

---

## Metrics Summary

| # | Metric | Status | Mining | Staking | Risk |
|---|--------|--------|--------|---------|------|
| 1 | Deregistration Risk | `HYPOTHESIS` | ✅ | ✅ | ✅ |
| 2 | Gini Coefficient | `PROVEN` | ✅ | ✅ | ✅ |
| 3 | Reward Distribution Model | `HYPOTHESIS` | ✅ | ✅ | ✅ |
| 4 | AMM Slippage Estimation | `HYPOTHESIS` | ✅ | ✅ | ✅ |
| 5 | ROI Estimation (Net TAO Yield) | `HYPOTHESIS` | ✅ | ✅ | ✅ |
| 6 | Taoflow Health | `NEEDS_VALIDATION` | ✅ | ✅ | ✅ |
| 7 | Rental Profitability | `HYPOTHESIS` | ✅ | ✅ | ✅ |
| 8 | Miner Churn | `HYPOTHESIS` | ✅ | ✅ | ✅ |
| 9 | Validator Landscape | `HYPOTHESIS` | ✅ | ✅ | ✅ |
| 10 | Validator Opportunity Assessment | `HYPOTHESIS` | ✅ | ✅ | ✅ |
| 11 | Competitive Density | `NEEDS_VALIDATION` | ✅ | ✅ | ✅ |
| 12 | Emission Trend | `PROVEN` | ✅ | ✅ | ✅ |
| 13 | Staking Yield (Net TAO Return) | `HYPOTHESIS` | ✅ | ✅ | ✅ |

---

## Metric 1: Deregistration Risk

**Status**: `HYPOTHESIS`
**Source**: `MetricsEngine.compute_deregistration_risk()`

### Hypothesis

On a full subnet, the miner with the lowest emission is replaced when
a new registrant arrives. Queue pressure (recent registrations) indicates
how actively people are trying to enter. Bottom 25% face real risk;
top 75% are safe unless registration pressure is extreme.

### Formula

```
IF num_uids < max_uids (subnet not full) → risk = 0.0 for all
IF miner is immune (blocks_since_reg < immunity_period) → risk = 0.0
queue_pressure = min(recent_registrations_24h / 10, 1.0)
IF miner in bottom 25% by emission:
    base_risk = 1.0 - (rank / bottom_quartile_size)
    risk = base_risk × (0.5 + 0.5 × queue_pressure)
ELSE: risk = 0.1 × queue_pressure × (1.0 - rank / total_miners)
```

**Output range**: "[0.0, 1.0] per miner"

### Usefulness

| Context | How it helps |
|---------|-------------|
| Mining | Tells you if entering a subnet is risky — high churn means you must be competitive immediately after immunity |
| Staking | Indirectly useful — high deregistration means competitive subnet (good for validators) |
| Risk | Track your own miner's risk score over time; exit before deregistration |

### Known Issues

- Bottom 25% threshold may not be correct for all subnets
- Queue pressure cap of 10 registrations/day may be too low for popular subnets
- pruning_score field exists in SDK docs but returns empty list in v10

### Assumptions to Validate

- [ ] Is the bottom 25% threshold correct?
- [ ] Does queue_pressure of 10/day represent max pressure?
- [ ] Will pruning_score become available in future SDK versions?

---

## Metric 2: Gini Coefficient

**Status**: `PROVEN`
**Source**: `MetricsEngine.compute_gini_coefficient()`

### Hypothesis

Standard economics measure of inequality. Measures how concentrated
rewards are among miners. Gini 0.9+ means almost all emission goes to
a few miners (WTA). Gini 0.3 means rewards are spread relatively evenly.

### Formula

```
Given sorted positive emissions [x₁, x₂, ..., xₙ] (ascending):
G = (2 × Σᵢ (i+1)×xᵢ) / (n × Σxᵢ) - (n+1)/n
Edge cases: empty/all-zero → 0.0, single value → 0.0
```

**Output range**: "[0.0, 1.0] — 0 = equality, 1 = one miner gets everything"

### Usefulness

| Context | How it helps |
|---------|-------------|
| Mining | High Gini = must be top-few or earn nothing. Low Gini = even mediocre miners earn. |
| Staking | High Gini subnets have more predictable top performers (stable for validators) |
| Risk | Primary input to Reward Distribution Model detection |

### Known Issues

None — standard formula

### Assumptions to Validate

- [ ] None — mathematically proven

---

## Metric 3: Reward Distribution Model

**Status**: `HYPOTHESIS`
**Source**: `MetricsEngine.detect_reward_distribution_model()`

### Hypothesis

Subnets fall into distinct reward patterns: WTA (top 3 capture >70%),
PROPORTIONAL (Gini < 0.5, all earn proportionally), TIERED (distinct
quality thresholds create step-function in emissions). Classification
determines what "winning" means on each subnet.

### Formula

```
top_3_share = sum(top 3 emissions) / sum(all emissions)
gini = compute_gini_coefficient(active_emissions)
IF top_3_share > 0.70 → WINNER_TAKES_ALL
ELIF gini < 0.5 → PROPORTIONAL
ELIF has_tiered_pattern (1-3 gaps with >50% drop) → TIERED
ELSE → UNKNOWN
```

**Output range**: "Enum {WINNER_TAKES_ALL, PROPORTIONAL, TIERED, UNKNOWN} + gini + top_3_concentration"

### Usefulness

| Context | How it helps |
|---------|-------------|
| Mining | Critical — on WTA you must be top-3 or earn nothing; on PROPORTIONAL even mediocre miners earn |
| Staking | WTA subnets have more predictable top performers → stable validator returns |
| Risk | Determines strategy — WTA requires excellence, PROPORTIONAL allows mediocrity |

### Known Issues

- 70% WTA threshold is an educated guess, not empirically derived
- Gini < 0.5 for PROPORTIONAL may be too generous
- Tiered pattern detection is heuristic (gap-based)

### Assumptions to Validate

- [ ] Is 70% the right WTA threshold?
- [ ] Is Gini < 0.5 the right PROPORTIONAL boundary?
- [ ] First live run confirmed 4/247 miners earn on SN1 — validates WTA detection

---

## Metric 4: AMM Slippage Estimation

**Status**: `HYPOTHESIS`
**Source**: `MetricsEngine._estimate_slippage()`

### Hypothesis

When selling alpha for TAO, the AMM pool moves against you. Larger sells
relative to pool size = more slippage. This is a CONSERVATIVE UPPER BOUND
because Bittensor also supports concentrated liquidity (Uniswap V3-style).

### Formula

```
pool_alpha = pool_tao / alpha_price
k = pool_tao × pool_alpha
new_pool_alpha = pool_alpha + sell_amount
actual_tao = pool_tao - (k / new_pool_alpha)
slippage = 1 - (actual_tao / (sell_amount × alpha_price))
```

**Output range**: "[0.0, 1.0] — 0 = no slippage, 1 = cannot sell"

### Usefulness

| Context | How it helps |
|---------|-------------|
| Mining | "Can I actually realize this yield?" — high slippage means paper yield > real yield |
| Staking | Same — validator dividends in alpha need conversion to TAO |
| Risk | Subnets with thin pools are risky even if yield looks good |

### Known Issues

- Conservative upper bound — real slippage may be lower with concentrated liquidity
- Doesn't account for multiple sells over time (pool recovers between trades)

### Assumptions to Validate

- [ ] Is constant-product the right model for Bittensor's base pool?
- [ ] How much does concentrated liquidity reduce actual slippage?

---

## Metric 5: ROI Estimation (Net TAO Yield)

**Status**: `HYPOTHESIS`
**Source**: `MetricsEngine.compute_roi_estimates()`

### Hypothesis

If you register on this subnet and perform at the average earning miner
level, this is what you'd earn. The alpha→TAO conversion via the AMM pool
determines your real return. Averages across EARNING miners only (emission > 0)
to avoid dilution by zero-earners on WTA subnets.

### Formula

```
miner_emissions = [n.emission for n if n.incentive > 0]  # already daily
avg_daily_alpha = sum(miner_emissions) / len(miner_emissions)
net_tao_yield_per_day = avg_daily_alpha × alpha_tao_price
days_to_recoup = registration_cost_tao / net_tao_yield_per_day
thirty_day_projection = (net_tao_yield × 30) - registration_cost
```

**Output range**: "net_tao_yield: [0, ∞) TAO/day; days_to_recoup: [0, ∞]; thirty_day: (-∞, ∞) TAO"

### Usefulness

| Context | How it helps |
|---------|-------------|
| Mining | Primary decision metric — "is this subnet worth entering?" |
| Staking | The validator variant (Metric 8) is the staking equivalent |
| Risk | days_to_recoup tells you how long your capital is at risk |

### Known Issues

- Average emission is misleading on WTA subnets (most earn 0, avg pulled up by top)
- No adjustment for YOUR likely rank position — assumes you'd be average
- Slippage estimate is conservative upper bound (ignores concentrated liquidity)

### Assumptions to Validate

- [ ] Does averaging across earning miners give a realistic estimate?
- [ ] Should we use median instead of mean for WTA subnets?
- [ ] Is constant-product AMM slippage model accurate for Bittensor pools?
- [ ] Is 5% over 7 days the right hold-vs-swap threshold?

---

## Metric 6: Taoflow Health

**Status**: `NEEDS_VALIDATION`
**Source**: `MetricsEngine.compute_taoflow_health()`

### Hypothesis

Under Bittensor's Taoflow model, subnets compete for stake. When stakers
leave (negative flow), emission share decreases, causing more stakers to
leave → death spiral. 3+ consecutive negative days = warning. 7+ days with
>25% emission decline = critical.

### Formula

```
daily_flows = [stake[i] - stake[i-1] for each day]
consecutive_negative = count from most recent backward
IF consecutive_negative >= 7 AND emission declined > 25%: DEATH_SPIRAL_RISK
ELIF consecutive_negative >= 3: DECLINING
ELSE: HEALTHY
```

**Output range**: "Enum {HEALTHY, DECLINING, DEATH_SPIRAL_RISK}"

### Usefulness

| Context | How it helps |
|---------|-------------|
| Mining | Don't enter a dying subnet — registration cost is wasted if emission drops to zero |
| Staking | CRITICAL — primary risk signal for validators. Death spiral = staked TAO earns less and less |
| Risk | Detect declining subnets early → exit before the crowd |

### Known Issues

- CURRENTLY DORMANT — always returns HEALTHY because we don't accumulate stake history yet
- Needs 7+ days of history before becoming meaningful
- Passes empty lists ([], []) in production

### Assumptions to Validate

- [ ] Is 3 days the right threshold for DECLINING?
- [ ] Is 25% emission decline the right threshold for DEATH_SPIRAL?
- [ ] To activate: accumulate daily total_validator_stake and total_emission per subnet

---

## Metric 7: Rental Profitability

**Status**: `HYPOTHESIS`
**Source**: `MetricsEngine.compute_rental_profitability()`

### Hypothesis

Mining is only worth it if rent_vs_buy_multiplier > 1.0 — meaning you earn
more TAO by mining than you could buy with the same money spent on GPU rental.
This accounts for the opportunity cost of renting.

### Formula

```
daily_rental_cost = cheapest_viable_gpu_hourly × 24
daily_tao_value_usd = net_tao_yield × tao_usd_price
daily_profit_usd = daily_tao_value_usd - daily_rental_cost
rent_vs_buy = net_tao_yield / (daily_rental_cost / tao_usd_price)
break_even_tao_price = daily_rental_cost / net_tao_yield
```

**Output range**: "rental_profitable: bool; rent_vs_buy_multiplier: [0, ∞)"

### Usefulness

| Context | How it helps |
|---------|-------------|
| Mining | THE decision metric for "should I rent a GPU to mine this subnet?" |
| Staking | Not directly relevant (validators don't need GPUs) |
| Risk | break_even_tao_price tells you how far TAO can drop before mining is unprofitable |

### Known Issues

- NOT CALLED IN PRODUCTION — requires hardware_tier from Stage 2 and cloud_pricing from external APIs
- Hardware tier mapping is static (hardcoded GPU configs per tier)
- Doesn't account for setup time, bandwidth costs, or spot instance interruptions

### Assumptions to Validate

- [ ] Are the tier-to-GPU mappings correct?
- [ ] Which cloud providers should be included? (RunPod, Vast.ai, Lambda Labs, AWS spot)
- [ ] How to keep pricing current?

---

## Metric 8: Miner Churn

**Status**: `HYPOTHESIS`
**Source**: `MetricsEngine.compute_miner_churn()`

### Hypothesis

High churn = competitive subnet where weak miners get replaced quickly.
Low churn = stable subnet where incumbents are entrenched. The trend tells
you if competition is heating up or cooling down.

### Formula

```
new_miners = current_hotkeys - previous_hotkeys
departed_miners = previous_hotkeys - current_hotkeys
churn_rate = (|new| + |departed|) / |current|
avg_lifespan = mean(current_block - block_at_registration) for active
net_change_pct = (|new| - |departed|) / |current|
IF net_change_pct > 0.05 → INCREASING
ELIF < -0.05 → DECREASING
ELSE → STABLE
```

**Output range**: "churn_rate: [0.0, 1.0]; trend: {INCREASING, STABLE, DECREASING}"

### Usefulness

| Context | How it helps |
|---------|-------------|
| Mining | High churn + INCREASING = dangerous (deregistered fast). Low churn + STABLE = incumbents safe. |
| Staking | High churn means more registration fees burned → good for subnet economics |
| Risk | DECREASING competition = opportunity window to enter |

### Known Issues

- Requires previous-day snapshot for comparison (first day has no baseline)
- Doesn't distinguish voluntary exit from deregistration

### Assumptions to Validate

- [ ] Is 5% net change the right threshold for INCREASING/DECREASING?
- [ ] Should we weight by emission (losing a top miner vs losing a zero-earner)?

---

## Metric 9: Validator Landscape

**Status**: `HYPOTHESIS`
**Source**: `MetricsEngine.compute_validator_landscape()`

### Hypothesis

The validator landscape determines how competitive staking is. A concentrated
subnet (one whale validator with >50% stake) means small stakers earn
proportionally less. A distributed subnet means more equal opportunity.

### Formula

```
validators = [n for n if n.dividends > 0]
total_stake = sum(v.stake)
top_1_share = max(v.stake) / total_stake
top_3_share = sum(top 3 stakes) / total_stake
concentrated = top_1_share > 0.5
net_yield = avg(v.emission) × alpha_tao_price
```

**Output range**: "active_validators: int; stake shares: [0, 1]; concentrated: bool"

### Usefulness

| Context | How it helps |
|---------|-------------|
| Mining | Concentrated validators may have biased scoring (single point of failure) |
| Staking | Avoid concentrated subnets where one whale dominates dividends |
| Risk | If dominant validator leaves, subnet scoring could change dramatically |

### Known Issues

- avg_validator_activity_blocks always returns 0 (blocks_since_last_step is subnet-level, not per-neuron)
- net_yield uses average emission which may be skewed by inactive validators

### Assumptions to Validate

- [ ] Is 50% the right threshold for "concentrated"?
- [ ] Does concentration actually reduce small-staker returns linearly?

---

## Metric 10: Validator Opportunity Assessment

**Status**: `HYPOTHESIS`
**Source**: `MetricsEngine.compute_validator_opportunity()`

### Hypothesis

Validators earn dividends proportional to their stake share. The minimum
effective stake tells you how much TAO you need to earn anything. The daily
ROI tells you the return rate on your capital. This is the KEY metric for
staking decisions.

### Formula

```
avg_emission = sum(v.emission) / len(validators)
net_tao_yield = avg_emission × alpha_tao_price
min_effective_stake = validators_by_dividends[10th percentile].stake
daily_roi = net_tao_yield / avg_stake
slots_available = max_validators - len(validators)
concentrated = max(v.stake) / total_stake > 0.5
```

**Output range**: "viable: bool; net_tao_yield: TAO/day; daily_roi_percent: %"

### Usefulness

| Context | How it helps |
|---------|-------------|
| Mining | Concentrated validators may have biased scoring |
| Staking | PRIMARY metric — directly answers "where should I stake my TAO?" |
| Risk | min_effective_stake tells you if your capital is sufficient |

### Known Issues

- Uses average emission which may be skewed
- Bottom 10% threshold for min_effective_stake is arbitrary
- Doesn't account for validator commission rates

### Assumptions to Validate

- [ ] Does avg_emission × alpha_price accurately predict validator earnings?
- [ ] Is bottom 10% the right minimum viable stake threshold?
- [ ] Does stake concentration affect returns linearly?
- [ ] Are there subnets where small validators earn disproportionately?

---

## Metric 11: Competitive Density

**Status**: `NEEDS_VALIDATION`
**Source**: `MetricsEngine.compute_competitive_density()`

### Hypothesis

More miners competing for the same emission pool = harder to earn.
This ratio captures "miners per unit of emission" in a normalized way.

### Formula

```
miners = [n for n if n.incentive > 0 or not n.is_validator]
earning_miners = count(m for m in miners if m.emission > 0)
total_emission = sum(m.emission)
density = earning_miners / (earning_miners + total_emission)
```

**Output range**: "[0.0, 1.0] — higher = more competition"

### Usefulness

| Context | How it helps |
|---------|-------------|
| Mining | High density = crowded, hard to stand out. Low density = less competition. |
| Staking | Indirectly useful — high competition may indicate a healthy subnet |
| Risk | Used as penalty factor in attractiveness score (weight 0.15) |

### Known Issues

- Mixes units (count + alpha/day) — normalization hack, not principled
- Consider replacing with occupancy rate or emission_per_miner in future

### Assumptions to Validate

- [ ] Is this formula meaningful or should we use a simpler alternative?
- [ ] Does this correlate with actual difficulty of earning on a subnet?

---

## Metric 12: Emission Trend

**Status**: `PROVEN`
**Source**: `MetricsEngine.compute_emission_trend()`

### Hypothesis

Emission trends indicate subnet health. Increasing emission = subnet is
gaining stake (Taoflow allocates more emission to subnets with more stake).
Declining = stakers are leaving.

### Formula

```
change_percent = (current - previous) / previous
IF change > 0.01 → "increasing"
ELIF change < -0.01 → "declining"
ELSE → "stable"
seven_day_trend = (day7 - day1) / day1  (when history available)
```

**Output range**: "direction: {increasing, stable, declining}; change_percent: (-∞, ∞)"

### Usefulness

| Context | How it helps |
|---------|-------------|
| Mining | Enter subnets with increasing emission (growing pie). Avoid declining. |
| Staking | Same — increasing emission means your stake earns more over time |
| Risk | Used in attractiveness score (weight 0.10) |

### Known Issues

None — simple day-over-day comparison

### Assumptions to Validate

- [ ] Is 1% the right threshold for "meaningful" change?
- [ ] Should we use exponential moving average instead of simple comparison?

---

## Metric 13: Staking Yield (Net TAO Return)

**Status**: `HYPOTHESIS`
**Source**: `MetricsEngine.compute_staking_yield()`

### Hypothesis

When you stake on a subnet, you buy alpha tokens and stake them.
Your dividend share is proportional to your stake (S = AS + TS).
Yield is earned in alpha, which must be converted back to TAO.
Net return accounts for entry slippage, exit slippage, and alpha price risk.

### Formula

```
alpha_bought = stake_tao / alpha_price × (1 - entry_slippage)
your_share = alpha_bought / (total_validator_alpha_stake + alpha_bought)
daily_alpha_yield = total_validator_emission_daily × your_share
daily_tao_net = daily_alpha_yield × alpha_price × (1 - exit_slippage)
apy = daily_tao_net × 365 / stake_tao × 100
break_even_depreciation = annual_yield / principal_value
```

**Output range**: "apy: (-100%, ∞); daily_tao_net: [0, ∞)"

### Usefulness

| Context | How it helps |
|---------|-------------|
| Mining | Not directly relevant |
| Staking | PRIMARY metric — answers "where should I stake my TAO for best return?" |
| Risk | break_even_depreciation shows how much alpha can drop before you lose money |

### Known Issues

- Assumes dividends proportional to S (validated on SN95 and SN1, D/S_share ≈ 1.0)
- Slippage is conservative upper bound (constant product AMM)
- Does not account for alpha price appreciation
- Uses current alpha price for exit

### Assumptions to Validate

- [ ] Dividends remain proportional to stake (validated live)
- [ ] Alpha price stays constant (conservative)
- [ ] You stake via alpha (confirmed: no direct TAO staking on subnets)

---

## How This Document Evolves

1. **Add/modify a metric** → Update the `Metric:` block in `metrics.py`
2. **Re-generate** → `python scripts/generate_metrics_reference.py`
3. **Validate against outcomes** → Change status from HYPOTHESIS to PROVEN
4. **Find a bug** → Fix the code, update the Metric: block, re-generate

The source of truth is always the code. This document is a readable view.

---

## Future: Configurable Metrics Registry

When the system reaches 20+ metrics or Stage 7 (OPTIMIZE) needs dynamic
control, refactor to a lightweight registry pattern. Until then, pure
functions in MetricsEngine + structured docstrings + this generator is
sufficient. See `kb/product-vision-roadmap.md` for the full plan.
